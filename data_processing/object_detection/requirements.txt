# RAM++ + GroundingDINO object detection dependencies.
# Install the CUDA PyTorch wheels first on the server, then install this file.

# Core deep learning framework. Kept here so pip sees already-installed CUDA
# wheels as satisfying the requirement.
torch>=2.3.0
torchvision>=0.18.0

# GroundingDINO through HuggingFace Transformers.
transformers==4.46.3
huggingface_hub>=0.23.0
tokenizers>=0.20.0
safetensors

# RAM++.
timm>=1.0.11
git+https://github.com/xinyu1205/recognize-anything.git

# Image/data/config utilities.
Pillow>=10.0.0
tqdm>=4.65.0
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0.0
PyYAML>=6.0.0
