# RAM++ + LocateAnything pipeline dependencies.
# Install torch/torchvision with the CUDA wheel matching the server driver first.

torch>=2.3.0
torchvision>=0.18.0

# LocateAnything official Transformers runtime
transformers==4.57.1
tokenizers==0.22.0
accelerate==1.5.2
peft==0.12.0
liger_kernel==0.3.1
safetensors
sentencepiece
einops
huggingface_hub

# RAM++
timm>=1.0.11
recognize-anything

# Image/data utilities
numpy==1.25.0
Pillow==11.1.0
opencv-python-headless==4.11.0.86
decord==0.6.0
tqdm>=4.65.0

# Optional on Linux if compatible with CUDA/PyTorch:
# flash-attn

