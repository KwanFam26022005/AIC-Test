extends: default.yaml

project:
  video_id: "L25_V001"
  frames_dir: "/content/L25_V001"
  output_dir: "/content/outputs/L25_V001"
  resume: true

runtime:
  device: "cuda:0"
  num_workers_io: 2

vlm:
  cache_path: "/content/outputs/L25_V001/vlm_cache.sqlite"

# Colab note:
# The reference notebook installs paddlepaddle-gpu from Paddle's CUDA 12.6 wheel index,
# then installs transformers/bitsandbytes for VLM. These stacks can pull different
# NVIDIA CUDA Python packages. For reliable Colab runs, execute PP-OCR and VLM in
# separate runtime sessions or reinstall the exact stack before each stage.

